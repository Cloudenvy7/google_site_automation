{
  "_why": "The deterministic layer. Skills are advisory; harness.py runs only if called. Hooks run on every tool call regardless of what code the agent wrote. See .claude/hooks/site_build_gate.py for what is enforced and the failure behind each rule.",
  "permissions": {
    "deny": [
      "Read(**/service_account.json)",
      "Read(./.env)",
      "Read(./.env.*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PROJECT_DIR}/.claude/hooks/site_build_gate.py\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
