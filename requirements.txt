# Direct dependencies only -- these are the three the code actually imports.
# Versions are the ones this system is proven on (Python 3.13.5, 2026-09-13);
# lower bounds rather than pins, because nothing here rides a narrow API.
#
#   google-api-python-client  Drive + Sheets
#   google-auth               service-account credentials
#   websocket-client          the Chrome DevTools Protocol socket
#
# Install:  python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt

google-api-python-client>=2.197.0
google-auth>=2.55.0
websocket-client>=1.9.0
